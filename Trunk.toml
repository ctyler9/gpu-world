[build]
# The page lives in web/; the crate it builds is at the repo root.
target = "web/index.html"
# wgpu/winit produce a large module; the release profile + wasm-opt keep it sane.
release = true

[serve]
addresses = ["127.0.0.1"]
port = 8080
open = false
